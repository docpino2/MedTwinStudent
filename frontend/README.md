# MedTwin Student Frontend

Next.js teacher dashboard for exploring the chest pain MVP, synthetic students, and adaptive reasoning output.

## Run

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_BASE_URL` if the backend is not running at `http://localhost:8000`.

